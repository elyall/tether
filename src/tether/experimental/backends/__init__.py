"""Experimental backends: tested against fakes, not yet against the service.

`tether add --kind <name>` and the extras work unchanged; `MATURITY =
"experimental"` on each class is what `tether backends` reports. See
:mod:`tether.experimental` for the graduation contract.
"""
